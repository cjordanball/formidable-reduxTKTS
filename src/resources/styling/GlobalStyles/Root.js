import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    globalContainer: {
        width: '100%'
    },
    safeContainer: {
        flex: 1,
        width: '100%'
    },
    bodyContainer: {
        
    }
})