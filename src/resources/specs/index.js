import musicStyles from '@app/resources/specs/musicStyles';
import colors from '@app/resources/specs/colors';

const Specs = {
    musicStyles,
    colors
}

export default Specs;
export const Colors = colors;
export const MusicStyles = musicStyles;
